- if @news
  %ul
    -@news.each do |n|
      %li
        %strong.subject
          =n['subject']
        %em.teacher
          %a{:href=>"http://sol.cs.hm.edu/fi/rest/public/person/name/#{n['teacher']}"}
            = get_dozent n['teacher']
        .text
          = n['text']
        -#%small
          gültig bis
          =n['expire'].strftime '%B %d, %Y'
- else
  #help
    %h3
      Usage:
    %code
      Just search in the box above for relevant teachers
    %br
    %a{:href=>"/?teacher=_all_"}show all news
      
